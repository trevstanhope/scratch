#include <stdio.h>
int sum;
int divisor;
int product;
int difference;
int remainder;
int main() {
  sum = (1 + 2);
  divisor = (4 / 2);
  product = (3 * 3);
  difference = (7 - 2);
  remainder = (7 % 2);
  printf("These are the values: %d %d %d %d %d", sum, divisor, product, difference, remainder);
}
